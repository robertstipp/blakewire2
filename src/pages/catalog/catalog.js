import React from "react"
import { Layout, Hero, Services, Algolia } from "../../components"

const Catalog = () => {
  return (
    <Layout>
      <Algolia />
    </Layout>
  )
}

export default Catalog
